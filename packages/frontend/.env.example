# Backend API base URL (must include the /api segment if backend mounts there).
# Leave unset in dev to use the default ("/api") which is forwarded by the
# Vite proxy in vite.config.ts to http://localhost:3000.
# Production value lives in .env.production.
VITE_API_BASE_URL=
